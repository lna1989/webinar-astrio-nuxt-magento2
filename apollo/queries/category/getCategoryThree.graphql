query categoryThree($filter: CategoryFilterInput){
  categoryList(filters: $filter){
    children_count
    children {
      id
      level
      name
      path
      url_path
      url_key
    }
  }
}
